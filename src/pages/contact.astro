---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact">
	<article class="container">
		<div class="hero-section">
			<h1>Let's build something</h1>
			<p class="lead">Need to automate workflows and reduce costs? We build AI systems that eliminate repetitive work and run on your infrastructure. Let's talk.</p>
		</div>

		<div class="split-layout">
			<div class="contact-info">
				<div class="contact-method">
					<span class="method-label">Email</span>
					<a href="mailto:hello@vl8.ai" class="method-value">hello@vl8.ai</a>
				</div>

				<div class="response-time">
					<p>We respond within 24 hours. First call is free. No sales pitch—just real talk about what you need.</p>
				</div>
			</div>

			<div class="calendly-wrapper">
				<!-- Calendly inline widget begin -->
				<div class="calendly-inline-widget" data-url="https://calendly.com/d/cthc-5jp-cmx?background_color=0a0a0a&text_color=ffffff&primary_color=ef4444" style="min-width:320px;height:1000px;"></div>
				<script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
				<!-- Calendly inline widget end -->
			</div>
		</div>
	</article>
</Layout>

<style>
	article {
		padding: 4rem 1.5rem;
		max-width: 1200px;
		margin: 0 auto;
	}

	.hero-section {
		margin-bottom: 5rem;
		text-align: center;
	}

	h1 {
		font-size: clamp(2.5rem, 7vw, 4.5rem);
		margin-bottom: 1.5rem;
		letter-spacing: -0.04em;
		font-weight: 700;
		line-height: 1.1;
	}

	.lead {
		font-size: clamp(1.1rem, 2.5vw, 1.5rem);
		line-height: 1.4;
		margin-bottom: 0;
		font-weight: 400;
		max-width: 40ch;
		margin: 0 auto;
		color: var(--color-text-muted);
	}

	.split-layout {
		display: grid;
		grid-template-columns: 1fr 1.5fr;
		gap: 4rem;
		align-items: center;
	}

	.contact-info {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.contact-method {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		align-items: flex-start;
	}

	.method-label {
		font-family: var(--font-mono);
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: var(--color-text-muted);
		font-weight: 600;
	}

	.method-value {
		font-size: clamp(1.5rem, 3vw, 2.5rem);
		font-weight: 600;
		letter-spacing: -0.02em;
		color: var(--color-text);
		text-decoration: none;
		transition: all 0.3s ease;
	}

	a.method-value {
		background: linear-gradient(to right, var(--color-accent), var(--color-accent));
		background-size: 0% 2px;
		background-repeat: no-repeat;
		background-position: left bottom;
		transition: background-size 0.3s ease, color 0.3s ease;
	}

	a.method-value:hover {
		color: var(--color-accent-bright);
		background-size: 100% 2px;
	}

	.response-time p {
		font-size: 1.1rem;
		line-height: 1.6;
		color: var(--color-text-muted);
		max-width: 40ch;
	}

	.calendly-inline-widget {
		width: 100%;
	}

	@media (max-width: 900px) {
		.split-layout {
			grid-template-columns: 1fr;
			gap: 3rem;
		}

		.contact-info {
			align-items: center;
			text-align: center;
		}

		.contact-method {
			align-items: center;
		}

		a.method-value {
			background-position: center bottom;
		}
	}

	@media (max-width: 768px) {
		article {
			padding: 2rem 1rem;
		}

		.hero-section {
			margin-bottom: 3rem;
		}
	}
</style>
